name: Example Models
description: A repository for example models

models:
  - models/UNet2dExample.model.yaml

transformations:
  - transformations/NormalizeZeroMeanUnitVariance.transform.yaml
  - transformations/Sigmoid.transformation.yaml

readers:
  - nN5.reader.yaml

samplers:
  - RandomBatch.samplers.yaml
